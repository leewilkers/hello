{
  "name": "lees-shelf",
  "version": "1.0.0",
  "description": "A personal link archive",
  "scripts": {
    "start": "eleventy --serve",
    "build": "rm -rf _site && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
