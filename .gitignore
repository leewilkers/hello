node_modules/
_site/
.cache/
.env
*.log
.DS_Store
.claude/
.mcp.json
POSITIONING_BRAINSTORM_*.md
source_reading/library/books/
source_reading/notes/
source_reading/acquire.html
source_reading/DOWNLOAD_CHECKLIST.md
_og-card.html
__pycache__/
scripts/*
!scripts/build-preview-mirror.js
.superpowers/
claude_design/
qa/
img/ornaments/_*
pattern-atlas/_source/
pattern-atlas/_seeds/

# Local-only cosmic audio (Symphonies + Jodrell Bank pulsars). Not for Pages deploy.
assets/nasa/audio/

# Local-only lab pages (404 on live; never deployed; no git history).
walk.njk
synth-lab.njk

# Local-only scratch (preview HTML + unused image extracts; not referenced
# anywhere in code, would deploy as fetchable URLs otherwise).
durer-extraction-preview.html
img/rabbits/durer-melencolia-extract-*.png
img/ornaments/two-man-saw-motif-detail.png

# Working notes (router passes, redlines, drafts). Eleventy would otherwise
# build these into public /consulting-*/, /writing-*/ pages.
consulting-router-pass-*.md
consulting-trim-redline-*.md
*-draft.md
pattern-atlas/alphabet.md

# Local-only APL wiki (/pattern-atlas/index/). Verbatim Alexander prose and
# 252 hero JPEGs from the epub — never deployed. Future "deploy with password"
# work will replace these lines, NOT delete them silently.
pattern-atlas/index/
pattern-atlas/patterns.json
pattern-atlas/clusters.json
_data/apl.js
_data/aplClusters.js
_includes/apl-wiki.njk
scripts/.readwise_shelf_ledger.json
